package patchachu

import (
	"testing"
)

func TestBuild(t *testing.T) {
	// Create a new datastore with db file
	// Create some instances and deployments to insert
	// Call Build() with the instances and deployments
	// Check that the db file exists
	// Check that the instances and deployments are in the db and that the mapping table is correct
	// Clean up by deleting the db file
}
